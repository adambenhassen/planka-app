# Keep the 1024px marketing icon in the same versioned tree as the rest of the
# App Store metadata. Credentials and the app identifier stay outside the repo.
app_icon('./fastlane/metadata/app_icon.png')
